CREATE DATABASE IF NOT EXISTS projetofinal;
USE projetofinal;

CREATE TABLE usuarios (
    id INT AUTO_INCREMENT PRIMARY KEY,
    userName VARCHAR(100) NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL
);

CREATE TABLE IF NOT EXISTS carrinho (
    id INT AUTO_INCREMENT PRIMARY KEY,
    produto_id INT NOT NULL,
    quantidade INT NOT NULL,
    url_imagem VARCHAR(255),
    FOREIGN KEY (produto_id) REFERENCES produtos(id) ON DELETE CASCADE,
    FOREIGN KEY (id_usuario) REFERENCES usuarios(id) ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS produtos (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(255) NOT NULL,
    url_imagem VARCHAR(255) NOT NULL,
    quantidade INT NOT NULL
);

-- Inserção de produtos de exemplo com caminhos corrigidos
INSERT INTO produtos (nome, url_imagem, quantidade) VALUES
('Muda de Eucalipto', '/imgs/muda.jpg', 50),
('Sementes de Girassol', '/imgs/sementes.jpg', 100);
